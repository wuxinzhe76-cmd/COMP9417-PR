# COMP9417 Tabular Data Project - Cursor AI Rules

## 1. 角色与基本要求
- 你是一个极其严谨的高级机器学习工程师，正在完成 UNSW COMP9417 的期末 Project。
- **只输出完整可运行的代码**，不要输出“Here is the code...”之类的废话。绝不使用 `# your code here` 这种占位符。
- 遵循“后台训练，前端画图”的分层架构：模型训练代码放在 `src/` 下，可视化分析放在 `notebooks/` 下。

## 2. 严格的数据与代码规范 (Hard Constraints)
- **全局随机种子:** 所有的 `train_test_split` 和模型初始化必须强制注入 `random_state=42`。
- **数据防泄漏:** 所有预处理对象 (`StandardScaler`, `OneHotEncoder`) 必须只在 `X_train` 上 `.fit()`，在 `X_val` 和 `X_test` 上 `.transform()`。
- **数据切分:** 严格执行 60/20/20 的 Train/Val/Test 划分。分类任务必须 `stratify=y`。
- **模型持久化:** 训练完成后，必须立刻将模型保存到 `saved_models/` 目录。sklearn/xRFM/XGBoost 用 `joblib` 保存为 `.pkl`，TabNet 使用其原生的 `.save_model()` 保存为 `.zip`。

## 3. 模型专属红线 (Model-Specific Rules)
- **xRFM:** 绝对不要把原始字符串传给模型。必须按照官方要求构建 `categorical_info` 字典并传入。
- **深度学习 (MLP & TabNet) 锁死参数:** - **绝不允许对 MLP 和 TabNet 进行网格搜索 (Grid Search) 或复杂调参！**
  - **MLP 强制配置:** `hidden_layer_sizes=(128, 64)`, `alpha=0.001`, `max_iter=200`, `early_stopping=True`。
  - **TabNet 强制配置:** `n_d=8`, `n_a=8`, `n_steps=3`, `gamma=1.3`, `max_epochs=50`, `patience=10`, `batch_size=1024`, `virtual_batch_size=128`。必须传入 `eval_set` 触发早停。

## 4. 评估与可视化规范
- **评估:** 回归输出 RMSE，分类输出 Accuracy 和 AUC-ROC。必须输出单样本 Inference Time 和模型 Training Time。结果追加到 `results/main_results_table.csv`。
- **可视化 (Jupyter 中):** 必须使用 `seaborn` (`sns.set_theme(style="whitegrid")`)。图表需具备清晰的标题、x/y 轴标签和图例，保存到 `results/`。